name: rideguyana
description: A new Flutter project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  # --- Core Flutter SDK ---
  flutter:
    sdk: flutter

  # --- Notification Sound ---
  just_audio: ^0.9.40        # stable version
  vibration: ^3.1.4

  # --- State Management ---
  flutter_riverpod: ^2.5.1   # ✅ FIXED — correct Riverpod version

  # --- Core Services ---
  http: ^1.2.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  intl: ^0.20.2

  # --- Location & Background ---
  geolocator: ^14.0.2
  permission_handler: ^12.0.1

  flutter_background_service: ^5.1.0
  flutter_background_service_android: ^6.3.1

  # --- Maps & Routing ---
  google_maps_flutter: ^2.12.3
  flutter_polyline_points: ^3.1.0

  # --- Firebase ---
  firebase_core: ^3.7.0
  firebase_auth: ^5.4.0
  cloud_firestore: ^5.5.0
  firebase_messaging: ^15.1.2

  # --- Networking ---
  dio: ^5.5.0
  socket_io_client: ^2.0.3+1
  connectivity_plus: ^5.0.2
  supabase_flutter: ^2.9.0
  universal_html: ^2.2.4
  web: ^1.1.1

  # --- UI & Design ---
  sizer: ^3.1.3
  flutter_svg: ^2.0.9
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.1
  fl_chart: ^0.69.0
  fluttertoast: ^8.2.4
  pinput: ^5.0.2
  sms_autofill: ^2.4.1
  image_picker: ^1.0.7
  flutter_local_notifications: ^17.2.3   # stable
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/icons/
